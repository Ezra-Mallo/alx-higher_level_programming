==========================================================================
How to  use 2-matrix_divided.py
==========================================================================

This module divides all elements of a matrix.

==========================================================================
``MyList`` can be instantiated with no arguments, which will return
an empty list:

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList()
    >>> type(my_list)
    <class '1-my_list.MyList'>

    >>> print(my_list)
    []


==========================================================================
a single argument,

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList([1, 2, 3])
    >>> print(my_list)
    [1, 2, 3]


==========================================================================
not sorted list,

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList([6, 3, 5])
    >>> print(my_list)
    [3, 5, 6]


==========================================================================

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList([12, 6, 3, 1])
    >>> print(my_list)
    [1, 3, 6, 12]


==========================================================================

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList(['Ezra', 'Ada', 'Ekene', 'Alx'])
    >>> print(my_list)
    ['Ada', 'Alx', 'Ekene', 'Ezra']


==========================================================================
